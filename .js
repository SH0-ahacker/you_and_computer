// in contruction!, this note is a info!, this is a script-test, and not a "perfect-script"!
// use this script for a chat in you and your computer......................................
<script>
// this is the configuration, don't delete this part, because, part deleted = co not run this game!!!!!!!!!!!!!!!
document.cookie = "user=human";
document.cookie = "internet=ok";
document.cookie = "#=#";
var day     = data.getDate();
var day2    = data.getDay();
var month     = data.getMonth(); 
var year    = data.getYear(); 
var fullyear    = data.getFullYear();  
var hours    = data.getHours();
var min     = data.getMinutes();
var seg     = data.getSeconds();
var mseg    = data.getMilliseconds();
var tz      = data.getTimezoneOffset();

</script>
<script>
alert("FULL RECOMENDATION:play a cool music for the best expirience of your life!")
let name = prompt("What is your name?");
  alert("Hello, " + name + "!");
  let humor = prompt("you are happy?");
  alert("ok!")
  let plan = prompt("what you plan to do?")
  alert("cool!")
  let idea1 = prompt("why do not you see some videos on youtube?,  " + name + "?")
  alert("ok, ;-; 👌")
  alert("hmmm...")
  alert("how about you sing a song? " + name + "?")
  let game = prompt("why do not you play a game " + name + "?")
alert("I know what time it is, the time is:" + Date() + name + "!")  
  

</script>
